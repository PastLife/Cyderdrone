# ระบบล็อกอินเจ้าหน้าที่

Supabase Auth (อีเมล + รหัสผ่าน) + RLS + ประวัติการใช้งาน
ระบบ **ไม่เปิดให้สมัครเอง** เจ้าหน้าที่ถูกเพิ่มจากหน้า `/admin/users` เท่านั้น

## ตั้งค่า 3 ขั้น

### 1. รัน migration

เปิด Supabase Dashboard → SQL Editor → วางเนื้อหาไฟล์
`supabase/migrations/20260808_officer_auth.sql` แล้วรัน

### 2. ใส่ค่า env

```bash
cp .env.example .env.local
```

แล้วเติมค่าจาก Supabase Dashboard → Project Settings → API

| ตัวแปร | เอามาจากไหน |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | publishable / anon key |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role key — **ห้ามหลุด ห้าม commit** |
| `NEXT_PUBLIC_SITE_URL` | URL ของเว็บ ใช้ในอีเมลตั้งรหัสผ่านใหม่ |

### 3. สร้างเจ้าหน้าที่คนแรก

ไก่กับไข่: หน้า `/admin/users` ต้องล็อกอินก่อนถึงจะเพิ่มคนได้
คนแรกจึงต้องสร้างจาก SQL Editor

```sql
select public.bootstrap_officer(
  'officer@agency.go.th',
  'รหัสผ่านอย่างน้อย12ตัว',
  'ชื่อ นามสกุล',
  'ตำแหน่ง'
);
```

จากนั้นเข้า `/login` แล้วเพิ่มคนที่เหลือผ่านหน้าเว็บ (มี audit log ครบ)

## ป้องกันกี่ชั้น

| ชั้น | ไฟล์ | กันอะไร |
| --- | --- | --- |
| 1. Middleware | `src/middleware.js` | ยังไม่ล็อกอิน → เด้งไป `/login` ก่อนโหลดหน้า |
| 2. Layout | `src/app/admin/layout.jsx` | เช็กว่าเป็นเจ้าหน้าที่ที่ยัง `active` |
| 3. API | `src/app/api/**/route.js` | ยิง API ตรง ๆ โดยไม่มี session → 401 |
| 4. RLS | Postgres | ต่อให้หลุดถึงฐานข้อมูล ก็อ่านไม่ได้ถ้า `is_active_officer()` เป็นเท็จ |

ชั้น 4 สำคัญที่สุด เพราะบังคับที่ฐานข้อมูล ไม่ใช่ที่โค้ดแอป

## จำกัดการล็อกอินผิด

`src/lib/loginGuard.js` — ปรับค่าได้ที่หัวไฟล์

- ผิดเกิน **5 ครั้ง** ต่ออีเมล → ล็อก **15 นาที** (นับจากครั้งที่ผิดล่าสุด)
- ผิดเกิน **15 ครั้ง** ต่อ IP → ล็อกทั้งเครือข่าย กันไล่ยิงหลายอีเมล
- ล็อกอินสำเร็จ → ล้างประวัติที่ผิดของอีเมลนั้นทิ้ง
- ข้อความ error ใช้ประโยคเดียวกันหมด ไม่บอกใบ้ว่าอีเมลมีในระบบไหม

## ประวัติการใช้งาน

ดูที่ `/admin/audit` — กรองตามประเภทได้ แบ่งหน้าละ 50 รายการ

บันทึก: ล็อกอินสำเร็จ/ล้มเหลว, ถูกล็อก, ล็อกเอาท์, อนุมัติ/ไม่อนุมัติคำขอ,
เปลี่ยนสถานะเบาะแส, เพิ่ม/ปิด/เปิดบัญชีเจ้าหน้าที่, ส่งลิงก์ตั้งรหัสใหม่

ตารางเป็น append-only — เจ้าหน้าที่อ่านได้อย่างเดียว เขียนได้เฉพาะเซิร์ฟเวอร์
ไม่มีทางลบผ่านหน้าเว็บ

## จัดการผู้ใช้งาน

`/admin/users` — ทุกคนที่ล็อกอินเข้าได้ (ตามที่ตกลงว่ายังไม่แยก role)

- เพิ่มเจ้าหน้าที่ พร้อมปุ่มสุ่มรหัสผ่าน
- ปิด/เปิดใช้งาน — ปิดแล้ว **เตะ session ที่ค้างอยู่ออกทันที** ไม่ต้องรอ token หมดอายุ
- ส่งลิงก์ตั้งรหัสผ่านใหม่ทางอีเมล
- ปิดบัญชีตัวเองไม่ได้ กันล็อกตัวเองออกจากระบบ

## ที่ยังไม่ได้ทำ

1. **แยก role** — ตอนนี้เจ้าหน้าที่ทุกคนสิทธิ์เท่ากัน รวมถึงจัดการผู้ใช้ได้ด้วย
   ถ้าจะแยก ให้เพิ่มคอลัมน์ `role` ใน `officers` แล้วทำ `is_admin()` คู่กับ `is_active_officer()`
2. **2FA** — Supabase Auth รองรับ TOTP อยู่แล้ว ยังไม่ได้เปิด
3. **หน้าเปลี่ยนรหัสผ่านของตัวเอง** — ตอนนี้ต้องใช้ลิงก์รีเซ็ตทางอีเมล
4. **Leaked password protection** — เปิดได้ที่ Dashboard → Authentication → Password
   จะเช็กรหัสผ่านกับ HaveIBeenPwned ให้
5. **ล้าง audit log เก่า** — ยังไม่มี retention policy ควรตั้งตามระยะที่ระเบียบหน่วยงานกำหนด
6. **`/permit/status` ยังเปิดสาธารณะ** — ใครรู้รหัส PMT- ก็ดูข้อมูลผู้ยื่นได้
   ตามดีไซน์เดิม แต่ควรพิจารณาว่าเปิดข้อมูลมากเกินไปไหมตาม PDPA
